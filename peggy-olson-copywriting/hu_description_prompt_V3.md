# HU Description Prompt — V3 (SUPERSEDED)

**Superseded 2026-09-20 by `hu_description_prompt_V4.md`. Use V4.**

⚠️ **Do not write from this file.** V3 never explicitly locked the point of view,
and output drifted into third-person, editorial-style summaries (`Bart explains
why...`, `he shares three principles...`). Bart flagged this 2026-09-20: *"It seems
the HU is outputting third-person summaries... this is not ideal. I want to change
it to first person for all platforms."* A live file that permits the copy failure
it's supposed to prevent is a trap, which is why this one is stubbed rather than
left in place.

**What V4 changed:**

1. §3 now states explicitly that all five outputs are written in first person, as
   Bart, with a banned/required example pair — not just implied through the
   "Write like Bart" voice note.
2. Every output section (§4–§8) repeats the first-person requirement at the point
   where it's easiest to violate, and the self-check gained a line item for it.

Everything else carried over unchanged — five distinct per-platform outputs, the
cleaned-VTT hard stop, chapters before the CTA, the graphology ban, no "link in
bio", no copy naming another company's platform, no markdown in the YouTube
description, exact VTT timestamps, and the `⚠️ TO VERIFY` discipline. V3 was in
good shape; it needed the POV rule stated outright, not a rewrite.

Kept as history only. Nothing reads this file.
