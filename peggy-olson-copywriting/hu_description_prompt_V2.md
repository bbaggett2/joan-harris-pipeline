# HU Description Prompt — V2 (SUPERSEDED)

**Superseded 2026-09-19 by `hu_description_prompt_V3.md`. Use V3.**

⚠️ **Do not write from this file.** V2 §5 instructed the Instagram caption to carry the line `Full breakdown on YouTube`. Bart banned that on 2026-09-19: *"I don't want any copy pushing the user to another platform that is owned by a different company."* A live file that tells the writer to produce banned copy is a trap, which is why this one is stubbed rather than left in place.

**What V3 changed:**

1. The `Full breakdown on YouTube` line is deleted, and the rule is stated generally in §2 — no HU copy names another company's platform as a destination. The only destination is HandwritingUniversity.com.
2. The companion pointer was corrected. V2 named `HU_Video_Publishing_Pipeline_V9.md`, which no longer exists; the lane is the step files in `joan-supervising-agent/hu-video-pipeline/`.

Everything else carried over unchanged — five distinct per-platform outputs, the cleaned-VTT hard stop, chapters before the CTA, the graphology ban, no "link in bio", no markdown in the YouTube description, exact VTT timestamps, and the `⚠️ TO VERIFY` discipline. V2 was in good shape; it needed one deletion, not a rewrite.

Kept as history only. Nothing reads this file.
